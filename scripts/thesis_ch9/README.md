# Thesis Chapter 9 — simulations and figures

Python (numpy, numba, scikit-learn, matplotlib with LaTeX) reimplementation of the
Chapter 7/5 models used for Chapter 9, "Trial-by-trial variability in the response of
spiking neural networks to stimulation". Run all scripts from this folder; outputs
go to `out/`, figures to `figs/` (copy them to `tese/figures/ch9/`).
Runtimes are wall-clock on 2 cores.

## Modules
| file | content |
|---|---|
| `ch9_models.py` | numba RK4 integrators: HH chain (V,m,h,n,s) and Izhikevich chain (v,u,s) with event-accurate reset (bisection on the whole-system substep); OU noise; route-D propagated signal |
| `ch9_circuits.py` | `Circuit`, `chain()`: Chapter 7 motifs (gap/chem links, V_th = presynaptic rest), equilibria, tau_0 |
| `ch9_protocol.py` | pump–probe with common random numbers, central difference G = [V(+q) − V(−q)]/2q |
| `ch9_analysis.py` | L2 norms, telescoping node/link factors, covariance shares, spike-time sensitivities |
| `ch9_network.py` | Chapter 5 network (all-to-all mean-field coupling), event-accurate resets, burst onsets, Kuramoto R, branching integrator with filtered mean field z |

## Experiments → thesis
| script | output | thesis |
|---|---|---|
| `run_e1_validation.py`, `check_izh_conv.py` | `e1_*.json`, `e1_linearity.npz` | Sec. 9.1 validation, Fig. 9.1c,d (~1 min) |
| `run_e2_chains.py hh` / `izh`, `summarize_e2.py` | `e2_*.npz`, `e2_hh_decomposition.json` | Figs. 9.1b, 9.2, 9.3, 9.4 (~3 min each) |
| `run_e3_continuity.py` | `e3_continuity.json` | Fig. 9.4f |
| `calib_noise.py`, `calib_izh.py` | stdout | noise level; Izhikevich chemical coupling threshold |
| `run_e4_conditioning.py` | `e4_conditioning.json`, `e4_{hh,izh}.npz` | Sec. 9.4, Fig. 9.5 (~3 min) |
| `run_e4_sigma_sweep.py hh`, `check_e4_lownoise.py` | `e4b_sigma_sweep_hh.json`, `e4_hh_lownoise_check.json` | Sec. 9.4 noise dependence |
| `run_e5_prepare.py 120 3` | `e5_state_{increasing,decreasing}_N120_s3.npz` | continuation protocol + 200 s per state (~9 min) |
| `run_e5_intermittent.py` | `e5_state_intermittent_N40_s2.npz` | N=40, 1500 s (~5 min) |
| `run_e5_probe.py 120 3 400 0.0125` | `e5_probe_*_N120_s3.npz` | Sec. 9.5 (~2 min) |
| `run_e5_probe_intermittent.py 700 0.0125` | `e5_probe_intermittent_N40_s2.npz` | Sec. 9.5 (~1 min) |
| `check_e5_linearity.py` | `e5_linearity_scaling.json` | eps scan, Sec. 9.5 |
| `analyze_e5.py` | `e5_analysis_*.json/npz` | Sec. 9.5 statistics, Fig. 9.6 |
| `make_figures.py [protocol maps decomposition models conditioning network]` | `figs/fig9_*.pdf` | all Chapter 9 figures |

`out/` here keeps only the JSON summaries and logs quoted in the text (large .npz
files are regenerated by the scripts). `out/ch9_numbers.json` consolidates Secs. 9.1–9.3.

## Notes
- Seeds are fixed in every script; results are reproducible up to floating-point
  differences between numba builds.
- The rho(S) values are cross-validated R^2 of HistGradientBoosting regressions with
  folds grouped by onset state (motifs) or 5-s blocks (network); they are lower bounds.
- Network runs are single realizations (N=120 seed 3; N=40 seed 2). N=60 seed 1 did not
  show intermittency within 1000 s and was not used.
